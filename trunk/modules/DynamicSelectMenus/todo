TRANSFERIR INTERNET / TESTAR PULL, PUSH, SSH SERVERS

não desabilitar campo ao change

somente listar escolas, bibliotecas se campo dependente Id > 0

Se usuário nivel biblioteca ou escola e vinculado mais uma biblioteca, mostrar select ao invez de texto



---

INTERFACE

  delete
      remover link deletar, mover logica para botão

  post
      adicionar coluna ação
          adicionar link cancelar quando situação == reservado
          implementar delete reserva botão cancelar
      renomear botão execute action para reservar

  tabela detalhes
    exibir qtd reservas em aberto
    exibir valor R$ multas em aberto

  filtro
      link para limpar cliente
      link para limpar obra
      validar (client side) se cliente informado
      validar (client side) se obra informada

API
  regras
    verificar no metódo getDataPrevistaDisponivelForExemplar se data prevista disponivel cai em feriado...

    can accept request
      - Cliente deve existir para biblioteca

    post reserva
      - exemplar mesma obra nao emprestado
      - qtd reservas em aberto <(= ?) limite biblioteca
      - valor R$ multas em aberto <(= ?) limite biblioteca
      - permitir reservar exemplares com situacao disponivel
      - corrigir data_prevista_disponivel 1 exemplar grava data atual

    pendencias, data prevista disponivel ultima pendencia com situacao reservado mostrando errado, mostrar data gravada?

  delete reserva (requer exemplar_id, cliente_id > inativar reservas em aberto do exemplar para o cliente )


habilitar try's interface reserva e frontend, api
